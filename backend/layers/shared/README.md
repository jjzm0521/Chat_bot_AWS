# Lambda Layer for shared dependencies
# This directory structure is required for Python Lambda layers

# To build the layer:
# pip install -r requirements.txt -t python/
# zip -r layer.zip python/

# The layer will be automatically built by CDK during deployment
